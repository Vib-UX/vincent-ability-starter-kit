{
  "name": "ability-lightning-payment",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/ability-lightning-payment/src",
  "projectType": "library",
  "targets": {
    "clean": {},
    "action:build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["pnpm node ./scripts/bundle-vincent-ability/esbuild.js"],
        "cwd": "packages/ability-lightning-payment",
        "parallel": false
      }
    },
    "action:only:deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "{projectRoot}"
      }
    },
    "action:deploy": {
      "executor": "nx:run-commands",
      "dependsOn": ["action:build"],
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "packages/ability-lightning-payment"
      }
    },
    "build": {
      "executor": "@nx/js:tsc",
      "dependsOn": ["action:build"],
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/ability-lightning-payment/dist",
        "main": "packages/ability-lightning-payment/src/index.ts",
        "tsConfig": "packages/ability-lightning-payment/tsconfig.lib.json",
        "assets": [
          "packages/ability-lightning-payment/*.md",
          {
            "glob": "**/*",
            "input": "packages/ability-lightning-payment/src/generated",
            "output": "src/generated"
          }
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "{projectRoot}/jest.config.js",
        "passWithNoTests": true
      }
    }
  }
}
